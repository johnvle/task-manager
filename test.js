console.log(`let's go super fast!`);

const incrementByOne = (x) => {
  return x + 1;
};

function incrementByTwo(x) {
  return x + 1;
}

console.log(incrementByTwo(4));

console.log(incrementByOne(2));